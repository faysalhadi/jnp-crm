Read src/App.js fully before making any changes.

This is refactor step 5. Extract small UI components only.
Do NOT change any logic. Do NOT move any modals or tabs.

STEP 5A — Create src/components/ui/Badge.js

Create a new file src/components/ui/Badge.js

Add this at the top:
  import React from "react";

Copy the exact Badge function from App.js:

  export default function Badge({ color, bg, children, small }) {
    // exact same code from App.js
  }

STEP 5B — Create src/components/ui/Spinner.js

Create a new file src/components/ui/Spinner.js

Add this at the top:
  import React from "react";

Copy the exact Spinner function from App.js:

  export default function Spinner() {
    // exact same code from App.js
  }

STEP 5C — Create src/components/ui/StageBar.js

Create a new file src/components/ui/StageBar.js

Add these at the top:
  import React from "react";
  import Badge from "./Badge";
  import { STAGES } from "../../constants";

Copy the exact StageBar function from App.js:

  export default function StageBar({ stageId }) {
    // exact same code from App.js
  }

STEP 5D — Update App.js

Add these imports at the top of App.js after other imports:
  import Badge from "./components/ui/Badge";
  import Spinner from "./components/ui/Spinner";
  import StageBar from "./components/ui/StageBar";

Then DELETE these functions from App.js:
  - Badge function
  - Spinner function
  - StageBar function

VERIFICATION CHECKLIST before building:
- Each component file has "import React from react" at top
- Badge.js has export default
- Spinner.js has export default
- StageBar.js has export default and imports Badge and STAGES
- All three are removed from App.js
- All three are imported in App.js
- No duplicate definitions anywhere

After changes:
1. Run npm run build
2. If errors fix them
3. Only when build is clean run:
   git add -A && git commit -m "refactor v2 step 5: extract UI components" 
   && git push origin main --force
4. Wait 2 minutes
5. Open https://jnp-crm.vercel.app and confirm it works
6. Tell me the new App.js line count