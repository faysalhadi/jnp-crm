Read src/App.js and src/context/StockContext.js 
fully before making any changes.

CHANGE 1 — Move showQuickSale and showUpgrade to StockContext

In src/context/StockContext.js add these state variables
inside StockProvider:
  const [showQuickSale, setShowQuickSale] = useState(false);
  const [quickSalePrefill, setQuickSalePrefill] = useState(null);
  const [showUpgrade, setShowUpgrade] = useState(false);
  const [upgradeTarget, setUpgradeTarget] = useState(null);

Add them to the StockContext.Provider value:
  showQuickSale, setShowQuickSale,
  quickSalePrefill, setQuickSalePrefill,
  showUpgrade, setShowUpgrade,
  upgradeTarget, setUpgradeTarget,

In App.js add these to the useStock() destructuring:
  showQuickSale, setShowQuickSale,
  quickSalePrefill, setQuickSalePrefill,
  showUpgrade, setShowUpgrade,
  upgradeTarget, setUpgradeTarget,

Then DELETE these from App.js:
  const [showQuickSale, setShowQuickSale] = useState(false);
  const [quickSalePrefill, setQuickSalePrefill] = useState(null);
  const [showUpgrade, setShowUpgrade] = useState(false);
  const [upgradeTarget, setUpgradeTarget] = useState(null);

CHANGE 2 — Clean up wrapper functions in App.js

Find these wrapper functions in App.js:
  function sendAskMessage(msg) { ... }
  function importSingleChatFile(file) { ... }
  function importMultipleChatFiles(files) { ... }
  function importWhatsAppChat() { ... }
  function exportData() { ... }

These are just wrappers around hook functions.
Check if they are still needed or if components
can call the hook functions directly.

For each one:
- If it just calls the hook function with same params,
  delete the wrapper and update any references to use
  the hook function directly
- If it adds extra logic, keep it

CHANGE 3 — Update DevicesView and other components
to get showQuickSale and showUpgrade from StockContext

In src/components/stock/DevicesView.js:
  Add: const { showQuickSale, setShowQuickSale,
    quickSalePrefill, setQuickSalePrefill,
    showUpgrade, setShowUpgrade,
    upgradeTarget, setUpgradeTarget } = useStock();
  Remove these from props destructuring.

In App.js find where DevicesView or StockTab is rendered.
Remove these props:
  showQuickSale={showQuickSale}
  setShowQuickSale={setShowQuickSale}
  quickSalePrefill={quickSalePrefill}
  setQuickSalePrefill={setQuickSalePrefill}
  showUpgrade={showUpgrade}
  setShowUpgrade={setShowUpgrade}
  upgradeTarget={upgradeTarget}
  setUpgradeTarget={setUpgradeTarget}

After changes:
1. Run npm run build
2. Fix ALL errors
3. git add -A && git commit -m "architecture: move remaining state to contexts" 
   && git push origin main --force
4. Test:
   - Stock tab works?
   - Can upgrade a device?
   - Quick sale works?
5. Tell me new App.js line count