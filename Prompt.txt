Read src/App.js, src/components/chat/ChatDetailView.js,
src/components/tabs/StockTab.js, and 
src/components/tabs/TradersTab.js fully before making changes.

CHANGE 1 — Remove returnToCustomer state from App.js

Find and remove these state declarations:
  const [returnToCustomerId, setReturnToCustomerId] = useState(null);
  const [returnToCustomerName, setReturnToCustomerName] = useState("");

Remove these props from wherever ChatDetailView is rendered:
  returnToCustomerId={returnToCustomerId}
  setReturnToCustomerId={setReturnToCustomerId}
  returnToCustomerName={returnToCustomerName}
  setReturnToCustomerName={setReturnToCustomerName}

Remove these props from wherever StockTab is rendered:
  returnToCustomerId={returnToCustomerId}
  setReturnToCustomerId={setReturnToCustomerId}
  returnToCustomerName={returnToCustomerName}
  setReturnToCustomerName={setReturnToCustomerName}

Remove these props from wherever TradersTab is rendered:
  returnToCustomerId={returnToCustomerId}
  setReturnToCustomerId={setReturnToCustomerId}
  returnToCustomerName={returnToCustomerName}
  setReturnToCustomerName={setReturnToCustomerName}

Also remove the setReturnToCustomerId and 
setReturnToCustomerName calls from all tab button 
onClick handlers in the bottom tab bar and desktop sidebar.

CHANGE 2 — Remove stage action buttons from ChatDetailView

In ChatDetailView.js find and remove the entire block:
  {/* Stage-specific action buttons */}
  {activeDeal && (() => {
    const stage = activeDeal.stage;
    ...
    return null;
  })()}

Also remove these from ChatDetailView props destructuring:
  setReturnToCustomerId,
  setReturnToCustomerName,
  setStockSearch,
  setStockFilter,
  setTraderSearch,

CHANGE 3 — Remove back button from StockTab

In StockTab.js find and remove the entire block:
  {returnToCustomerId && (
    <div style={{...}}>
      ...
      ← Back to {returnToCustomerName}
    </div>
  )}

Also remove these from StockTab props destructuring:
  returnToCustomerId,
  setReturnToCustomerId,
  returnToCustomerName,
  setReturnToCustomerName,
  setActiveCustomerId,
  setActiveDealId,
  setView,
  customers,

Only remove them if they are not used elsewhere in StockTab.

CHANGE 4 — Remove back button from TradersTab

In TradersTab.js find and remove the entire block:
  {returnToCustomerId && (
    <div style={{...}}>
      ...
      ← Back to {returnToCustomerName}
    </div>
  )}

Also remove these from TradersTab props destructuring:
  returnToCustomerId,
  setReturnToCustomerId,
  returnToCustomerName,
  setReturnToCustomerName,
  setActiveCustomerId,
  setActiveDealId,
  setView,
  setActiveTab,
  customers,

Only remove them if they are not used elsewhere in TradersTab.

After ALL changes:
1. Run npm run build
2. Fix ALL errors
3. git add -A && git commit -m "revert: remove stage action buttons and back to client feature" 
   && git push origin main --force
4. Test:
   - Open a client chat — no Find This Device buttons
   - Go to Stock tab — no back button at top
   - Go to Traders tab — no back button at top
   - Everything else still works normally