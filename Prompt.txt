Read src/SourcingModule.js fully before making any changes.

This is architecture rewrite Step 9.
We are splitting SourcingModule.js into smaller files.
Do NOT change any logic. Only move code into new files.

STEP 9A — Create src/components/sourcing/SourcingHelpers.js

Create this file with all shared helpers and small components:

  import React from "react";

  export const DEFAULT_RATE = 3.67;
  export const DUTY_PCT = 0.05;

  export const STAGES = [
    // exact same STAGES array from SourcingModule.js
  ];
  export const STAGE_MAP = Object.fromEntries(
    STAGES.map(s => [s.id, s])
  );
  export const MILESTONES = {
    // exact same MILESTONES object from SourcingModule.js
  };

  export const fmtUSD = n => n 
    ? "$" + Number(n).toLocaleString("en-US", 
        { maximumFractionDigits: 0 }) 
    : "—";
  export const fmtAED = n => (n || n === 0) 
    ? "AED " + Number(n).toLocaleString("en-US", 
        { maximumFractionDigits: 0 }) 
    : "—";

  export function hoursUntil(ts) {
    // exact same code
  }
  export function timeAgo(ts) {
    // exact same code
  }
  export function fmtTime(ts) {
    // exact same code
  }
  export function calcLanded(d, rate) {
    // exact same code
  }
  export function calcProfit(d, rate) {
    // exact same code
  }
  export async function callClaude(apiKey, prompt, system) {
    // exact same code
  }
  export function StageBadge({ status }) {
    // exact same code
  }
  export function MilestoneBadge({ milestone }) {
    // exact same code
  }
  export function Row({ label, value, bold, color }) {
    // exact same code
  }

STEP 9B — Create src/components/sourcing/DealDetail.js

Extract the DealDetail function (lines 121-958) 
into its own file:

  import React, { useState, useEffect, useRef } from "react";
  import { supabase } from "../../supabase";
  import { 
    STAGES, STAGE_MAP, MILESTONES, DEFAULT_RATE, DUTY_PCT,
    fmtUSD, fmtAED, hoursUntil, timeAgo, fmtTime,
    calcLanded, calcProfit, callClaude,
    StageBadge, MilestoneBadge, Row
  } from "./SourcingHelpers";
  import SourcingCalculator from "./SourcingCalculator";
  import SourcingMessages from "./SourcingMessages";

  export default function DealDetail({ 
    deal: initialDeal, suppliers, rate, 
    anthropicKey, onBack, onUpdate 
  }) {
    // exact same code from lines 121-958
  }

STEP 9C — Create src/components/sourcing/SupplierComponents.js

Extract these smaller components into one file:
  - DealCard (lines 959-1035)
  - NewDealModal (lines 1036-1153)
  - GmailSheet (lines 1154-1219)
  - SectionToggle (lines 1220-1263)
  - SupplierDetail (lines 1264-1408)
  - SuppliersList (lines 1409-1508)
  - AddSupplierModal (lines 1509-end)

  import React, { useState, useEffect } from "react";
  import { supabase } from "../../supabase";
  import {
    STAGES, STAGE_MAP, DEFAULT_RATE, DUTY_PCT,
    fmtUSD, fmtAED, timeAgo, calcLanded, calcProfit,
    callClaude, StageBadge, Row
  } from "./SourcingHelpers";

  export function DealCard({ deal: d, rate, onClick }) {
    // exact same code
  }
  export function NewDealModal({ suppliers, rate, onClose, onCreate }) {
    // exact same code
  }
  export function GmailSheet({ anthropicKey, onClose, onCreateDeal }) {
    // exact same code
  }
  export function SectionToggle({ section, setSection, deals, suppliers }) {
    // exact same code
  }
  export function SupplierDetail({ supplier, deals, rate, onBack, onSelectDeal, onUpdate }) {
    // exact same code
  }
  export function SuppliersList({ suppliers, deals, rate, onSelect, onAdd }) {
    // exact same code
  }
  export function AddSupplierModal({ onClose, onCreate }) {
    // exact same code
  }

STEP 9D — Update SourcingModule.js to use new files

In SourcingModule.js:

1. Add these imports:
   import DealDetail from "./components/sourcing/DealDetail";
   import {
     DealCard, NewDealModal, GmailSheet, SectionToggle,
     SupplierDetail, SuppliersList, AddSupplierModal
   } from "./components/sourcing/SupplierComponents";
   import {
     STAGES, STAGE_MAP, MILESTONES, DEFAULT_RATE, DUTY_PCT,
     fmtUSD, fmtAED, hoursUntil, timeAgo, fmtTime,
     calcLanded, calcProfit, callClaude,
     StageBadge, MilestoneBadge, Row
   } from "./components/sourcing/SourcingHelpers";

2. DELETE all extracted code from SourcingModule.js:
   - STAGES, STAGE_MAP, MILESTONES constants
   - DEFAULT_RATE, DUTY_PCT constants
   - fmtUSD, fmtAED, hoursUntil, timeAgo, fmtTime functions
   - calcLanded, calcProfit, callClaude functions
   - StageBadge, MilestoneBadge, Row components
   - DealDetail function (entire 837 lines)
   - DealCard function
   - NewDealModal function
   - GmailSheet function
   - SectionToggle function
   - SupplierDetail function
   - SuppliersList function
   - AddSupplierModal function

3. SourcingModule.js should now only contain:
   - The main SourcingModule export function
   - State declarations
   - Data loading functions
   - Main return JSX using the imported components

IMPORTANT RULES:
- Every new file must have import React from "react" at top
- Do not change any logic
- Make sure all imports are correct in each file
- SourcingCalculator.js and SourcingMessages.js 
  already exist — do not recreate them
- SourcingDealList.js already exists — do not recreate it

After changes:
1. Run npm run build
2. Fix ALL errors one by one
3. Only when build is clean:
   git add -A && git commit -m "architecture step 9: split SourcingModule components" 
   && git push origin main --force
4. Wait 2 minutes
5. Open https://jnp-crm.vercel.app
6. Test Sourcing tab:
   - Does supplier list show?
   - Does deal detail open?
   - Does stage change work?
   - Does WhatsApp paste work?
   - Does Gmail sync work?
7. Tell me new SourcingModule.js line count