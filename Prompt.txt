Read src/SourcingModule.js fully before making any changes.

This is architecture rewrite Step 9.
We are splitting SourcingModule.js into smaller files.
Do NOT change any logic. Only split into separate files.

First run this to see the structure:
grep -n "^function \|^const.*=.*(" src/SourcingModule.js | head -30

STEP 9A — Identify the main sections in SourcingModule.js

SourcingModule.js likely has these sections:
1. State declarations and data loading
2. Deal list view
3. Deal detail view  
4. Landed cost calculator
5. Message/timeline section
6. Reply generator

STEP 9B — Create src/components/sourcing/SourcingDealList.js

Extract the deal list view section into its own component.
This is the part that shows all deals grouped by stage.

Create src/components/sourcing/SourcingDealList.js:
  import React from "react";
  import { supabase } from "../../supabase";

  export default function SourcingDealList({
    deals, suppliers, selectedDeal, setSelectedDeal,
    showAddDeal, setShowAddDeal, dealForm, setDealForm,
    saveDeal, deleteDeal, loadDeals,
    SOURCING_STAGES, SOURCING_STAGE_COLORS,
  }) {
    // exact same JSX for the deal list
  }

STEP 9C — Create src/components/sourcing/SourcingCalculator.js

Extract the landed cost calculator into its own component.

Create src/components/sourcing/SourcingCalculator.js:
  import React, { useState } from "react";

  export default function SourcingCalculator({ deal }) {
    // exact same calculator JSX and logic
  }

STEP 9D — Create src/components/sourcing/SourcingMessages.js

Extract the message timeline section into its own component.

Create src/components/sourcing/SourcingMessages.js:
  import React, { useState } from "react";
  import { supabase } from "../../supabase";
  import { callClaude } from "../../utils/claude";

  export default function SourcingMessages({
    deal, supplier, anthropicKey,
    messages, setMessages, loadMessages,
    showReplyGen, setShowReplyGen,
    replyGmail, setReplyGmail,
    replyWA, setReplyWA,
    replyLoading, setReplyLoading,
  }) {
    // exact same messages/timeline JSX and logic
  }

STEP 9E — Update SourcingModule.js to use sub-components

In SourcingModule.js replace the extracted sections
with the new components:

  import SourcingDealList from "./components/sourcing/SourcingDealList";
  import SourcingCalculator from "./components/sourcing/SourcingCalculator";
  import SourcingMessages from "./components/sourcing/SourcingMessages";

Replace each section in the JSX with the component.

IMPORTANT RULES:
- Every new file must have import React from "react" at top
- Do not change any logic
- Pass all required state as props
- Test Gmail sync still works
- Test WhatsApp paste still works
- Test calculator still works
- Test stage changes still work

After changes:
1. Run npm run build
2. Fix ALL errors one by one
3. Only when build is clean:
   git add -A && git commit -m "architecture step 9: split SourcingModule" 
   && git push origin main --force
4. Wait 2 minutes
5. Open https://jnp-crm.vercel.app
6. Test Sourcing tab thoroughly:
   - Does deal list show?
   - Does deal detail open?
   - Does calculator work?
   - Does stage change work?
   - Does WhatsApp paste work?
7. Tell me new SourcingModule.js line count