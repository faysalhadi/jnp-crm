Read src/components/tabs/StockTab.js fully before making changes.

Find the import from constants at the top of StockTab.js:
  import { BRANDS, EMPTY_STOCK } from "../../constants";

Replace it with:
  import { BRANDS, EMPTY_STOCK, PART_ICONS, PART_CATEGORIES } from "../../constants";

Also check if PART_CATEGORIES is used in StockTab.js 
and add it if missing from the import.

Then check src/constants/index.js and confirm that 
PART_ICONS and PART_CATEGORIES are exported there.
If they are not, add them:

export const PART_CATEGORIES = [
  "RAM", "SSD", "HDD", "Screen", "Battery", 
  "Charger", "Keyboard", "Trackpad", "Other"
];

export const PART_ICONS = { 
  RAM: "🧠", SSD: "💾", HDD: "💿", Screen: "🖥️", 
  Battery: "🔋", Charger: "🔌", Keyboard: "⌨️", 
  Trackpad: "🖱️", Other: "🔧" 
};

After changes:
1. Run npm run build
2. Fix any errors
3. git add -A && git commit -m "fix: import PART_ICONS and PART_CATEGORIES in StockTab" 
   && git push origin main --force
4. Open https://jnp-crm.vercel.app
5. Go to Stock tab → click Parts toggle
6. Tell me if it works