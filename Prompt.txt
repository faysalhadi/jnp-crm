Read src/App.js fully before making any changes.

Find line 345 in App.js where setChatMode is used.
Look at the surrounding code to understand what it does.

Since chatMode and setChatMode were removed as they were
"kept for compat" — find every reference to chatMode 
and setChatMode in App.js and remove them completely.

Also check these files for any reference to chatMode 
or setChatMode and remove those too:
- src/hooks/useChatActions.js
- src/components/chat/ChatDetailView.js
- src/components/chat/InputBar.js

After changes:
1. Run npm run build
2. Fix any remaining errors the same way —
   if something is not defined, find where it 
   was deleted and either remove the reference 
   or add it back to the appropriate context
3. git add -A && git commit -m "fix: remove chatMode references causing blank screen" 
   && git push origin main --force
4. Tell me if app opens and new App.js line count