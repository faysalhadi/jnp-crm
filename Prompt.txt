Read src/components/tabs/HomeTab.js fully before making changes.

Find this line in HomeTab.js:
  {overdueFollowUps > 0 && <button onClick={() => setActiveTab("tasks")}

Replace the onClick with:
  onClick={() => { setActiveTab("customers"); setFilter("overdue"); }}

Also find the Follow Ups stat card in the stats row.
It looks like:
  { label: "Follow Ups", value: followUpsDue, ... }

Make sure it also has an onClick:
  onClick={() => { setActiveTab("customers"); setFilter("overdue"); }}

Add cursor pointer to that stat card style.

Also check if setFilter is being passed as a prop to HomeTab.
Find where HomeTab is rendered in App.js and confirm 
setFilter is passed:
  setFilter={setFilter}

If it is not passed add it.

After changes:
1. Run npm run build
2. Fix any errors
3. git add -A && git commit -m "fix: follow ups button navigates to overdue clients" 
   && git push origin main --force
4. Test: tap Follow Ups on dashboard — 
   does it go to Contacts tab showing overdue clients?