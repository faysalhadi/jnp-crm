Read src/components/chat/ChatDetailView.js and
src/components/chat/ChatHeader.js fully before changes.

CHANGE 1 — Remove receipt generator from client chat

In ChatHeader.js find and remove:
1. The Receipt button — looks like:
   <button onClick={() => setShowReceipt(true)}
   or similar. Remove the entire button.

2. The Receipt modal/panel — looks like:
   {showReceipt && (
     <div ...>
       ...receipt content...
     </div>
   )}
   Remove the entire block.

3. Remove these from ChatHeader props:
   showReceipt, setShowReceipt,
   receiptPaymentMethod, setReceiptPaymentMethod,
   buildReceiptText,
   saveReceiptNumber,

In ChatDetailView.js remove these from props 
destructuring and from the ChatHeader usage:
   showReceipt, setShowReceipt,
   receiptPaymentMethod, setReceiptPaymentMethod,
   buildReceiptText,
   saveReceiptNumber,

In App.js find where ChatDetailView is rendered.
Remove these props:
   showReceipt={showReceipt}
   setShowReceipt={setShowReceipt}
   receiptPaymentMethod={receiptPaymentMethod}
   setReceiptPaymentMethod={setReceiptPaymentMethod}
   buildReceiptText={buildReceiptText}
   saveReceiptNumber={saveReceiptNumber}

Also remove these state declarations from App.js
if they are only used for the client chat receipt:
   const [showReceipt, setShowReceipt] = useState(false);
   const [receiptPaymentMethod, setReceiptPaymentMethod] = useState("Cash");

CHANGE 2 — Make sure receipt still works in Sales History

The Sales History tab already has a 🧾 Receipt button
on each sale card that opens the receipt modal.
This should stay exactly as is — do not touch it.

CHANGE 3 — Make sure receipt works after completing reservation

In src/components/stock/ReservedView.js find the
Complete Sale button flow. After a sale is completed
it should trigger the receipt.

Find where setSaleReceiptData and setShowSaleReceipt
are called after completing a reservation.
Make sure these calls still exist and work correctly.

After changes:
1. Run npm run build
2. Fix any errors
3. git add -A && git commit -m "feat: remove receipt from client chat, keep in sales history" 
   && git push origin main --force
4. Test:
   - Open a client chat with closed deal
   - Is receipt button gone?
   - Go to Sales History — does receipt button still work?
   - Complete a reservation — does receipt appear?
5. Tell me new ChatDetailView.js line count