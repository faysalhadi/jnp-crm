Read src/App.js and src/components/layout/BroadcastModal.js
fully before making any changes.

CHANGE 1 — Update BroadcastModal to use contexts

In src/components/layout/BroadcastModal.js add:
  import { useUI } from "../../context/UIContext";
  import { useCustomers } from "../../context/CustomerContext";

Create a useBroadcast context OR move broadcast state
to UIContext.

Actually — simplest fix is to move broadcast state
to a new file src/context/BroadcastContext.js:

  import React, { createContext, useContext, 
    useState } from "react";

  const BroadcastContext = createContext(null);

  export function BroadcastProvider({ children }) {
    const [showBroadcast, setShowBroadcast] = useState(false);
    const [broadcastItem, setBroadcastItem] = useState(null);
    const [broadcastClients, setBroadcastClients] = useState([]);
    const [broadcastSelected, setBroadcastSelected] = 
      useState(new Set());
    const [broadcastMessages, setBroadcastMessages] = useState([]);
    const [broadcastLoading, setBroadcastLoading] = useState(false);
    const [broadcastStep, setBroadcastStep] = useState("clients");
    const [broadcastSent, setBroadcastSent] = useState(new Set());

    return (
      <BroadcastContext.Provider value={{
        showBroadcast, setShowBroadcast,
        broadcastItem, setBroadcastItem,
        broadcastClients, setBroadcastClients,
        broadcastSelected, setBroadcastSelected,
        broadcastMessages, setBroadcastMessages,
        broadcastLoading, setBroadcastLoading,
        broadcastStep, setBroadcastStep,
        broadcastSent, setBroadcastSent,
      }}>
        {children}
      </BroadcastContext.Provider>
    );
  }

  export function useBroadcast() {
    const context = useContext(BroadcastContext);
    if (!context) throw new Error(
      "useBroadcast must be used within BroadcastProvider"
    );
    return context;
  }

Add BroadcastProvider to index.js:
  import { BroadcastContext } from './context/BroadcastContext';

  root.render(
    <AuthProvider>
      <UIProvider>
        <BroadcastProvider>
          <SalesProvider>
            <PartsProvider>
              <CustomerProvider>
                <TradersProvider anthropicKey={
                  localStorage.getItem("jnp_anthropic_key") || ""
                }>
                  <ReservationsProvider>
                    <StockProvider>
                      <ChatProvider>
                        <ImportProvider>
                          <AskClaudeProvider>
                            <App />
                          </AskClaudeProvider>
                        </ImportProvider>
                      </ChatProvider>
                    </StockProvider>
                  </ReservationsProvider>
                </TradersProvider>
              </CustomerProvider>
            </PartsProvider>
          </SalesProvider>
        </BroadcastProvider>
      </UIProvider>
    </AuthProvider>
  );

In BroadcastModal.js:
  import { useBroadcast } from "../../context/BroadcastContext";
  
  Add at top of component:
    const {
      showBroadcast, setShowBroadcast,
      broadcastItem, setBroadcastItem,
      broadcastClients, setBroadcastClients,
      broadcastSelected, setBroadcastSelected,
      broadcastMessages, setBroadcastMessages,
      broadcastLoading, setBroadcastLoading,
      broadcastStep, setBroadcastStep,
      broadcastSent, setBroadcastSent,
    } = useBroadcast();

  Remove ALL props from BroadcastModal.
  Keep generateBroadcastMessages as a prop since
  it comes from useBroadcast hook in App.js.

  Actually move generateBroadcastMessages to 
  BroadcastContext too — import useBroadcast hook
  from hooks/useBroadcast.js inside BroadcastModal.

In App.js replace:
  <BroadcastModal
    showBroadcast={showBroadcast} ...many props...
    generateBroadcastMessages={generateBroadcastMessages}
  />

With just:
  <BroadcastModal />

Remove all broadcast state from App.js useHook 
destructuring since BroadcastModal now gets them 
directly from context.

CHANGE 2 — Move ContactModal logic to context

In App.js find ContactModal usage:
  {showContactModal && (
    <ContactModal
      defaultType={contactModalPreType}
      onClose={() => {...}}
      onCreated={async (customer, deal) => {...}}
    />
  )}

The onCreated callback uses setActiveCustomerId,
setActiveDealId, setView, setActiveTab, loadCustomers.
All of these are now in contexts.

Extract ContactModal into its own wrapper component
src/components/modals/ContactModalWrapper.js:

  import React from "react";
  import { useCustomers } from "../../context/CustomerContext";
  import { useUI } from "../../context/UIContext";
  import ContactModal from "../ContactModal";

  Wait — ContactModal.js is at src/ContactModal.js
  So import path would be:
  import ContactModal from "../../ContactModal";

  export default function ContactModalWrapper() {
    const { 
      showContactModal, setShowContactModal,
      contactModalPreType, setContactModalPreType,
      setActiveCustomerId, setActiveDealId,
      loadCustomers, setView,
    } = useCustomers();
    const { setActiveTab } = useUI();

    if (!showContactModal) return null;

    return (
      <ContactModal
        defaultType={contactModalPreType}
        onClose={() => { 
          setShowContactModal(false); 
          setContactModalPreType(null); 
        }}
        onCreated={async (customer, deal) => {
          await loadCustomers();
          setShowContactModal(false);
          setContactModalPreType(null);
          if (customer) {
            setActiveCustomerId(customer.id);
            setActiveDealId(deal?.id || null);
            setView("detail");
            setActiveTab("customers");
          }
        }}
      />
    );
  }

In App.js replace the ContactModal block with:
  import ContactModalWrapper from 
    "./components/modals/ContactModalWrapper";
  
  <ContactModalWrapper />

CHANGE 3 — Remove remaining unused state from App.js

After all above changes check App.js for any remaining
state declarations that are now in contexts.
Delete any duplicates found.

CHANGE 4 — Clean up unused imports in App.js

Remove any imports in App.js that are no longer used
after moving state and functions to contexts.

After ALL changes:
1. Run npm run build
2. Fix ALL errors one by one
3. git add -A && git commit -m "architecture final: BroadcastContext, ContactModalWrapper" 
   && git push origin main --force
4. Wait 2 minutes
5. Open https://jnp-crm.vercel.app
6. Test:
   - Add a new contact — does it work?
   - Broadcast from stock item — does it work?
   - All modals open correctly?
7. Tell me final App.js line count