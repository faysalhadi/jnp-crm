Read src/context/PartsContext.js, src/App.js and 
src/components/stock/PartsView.js before making changes.

CHANGE 1 — Wrap loadPartsSold in useCallback in PartsContext

In PartsContext.js find:
  async function loadPartsSold() {

Replace with:
  const loadPartsSold = useCallback(async () => {
    setPartsSoldLoading(true);
    const { data } = await supabase
      .from("parts_sales")
      .select("*")
      .order("sold_at", { ascending: false });
    setPartsSold(data || []);
    setPartsSoldLoading(false);
  }, []);

Make sure useCallback is imported at the top of PartsContext.js.

CHANGE 2 — Remove loadPartsSold from PartsView.js

In PartsView.js find where loadPartsSold is called.
Remove any useEffect that calls loadPartsSold.
PartsView should NOT load parts sold data itself.
App.js handles this via the stockFilter useEffect.

Remove loadPartsSold from the useParts destructuring
in PartsView.js.

CHANGE 3 — Keep loadPartsSold only in App.js useEffect

In App.js make sure this is the only place 
loadPartsSold is called:
  useEffect(() => {
    if (stockFilter !== "parts_sold") return;
    loadPartsSold();
  }, [stockFilter, loadPartsSold]);

After changes:
1. Run npm run build
2. Fix any errors
3. git add -A && git commit -m "fix: parts sold no longer flickers" 
   && git push origin main --force
4. Test:
   - Stock → Parts → Parts Sold filter
   - Does it load once without flickering?
   - Does it show parts sold list?
5. Tell me if it works and StockTab.js line count