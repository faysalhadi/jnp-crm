Read src/App.js and src/components/tabs/StockTab.js 
fully before making any changes.

CHANGE 1 — Remove showToast prop from StockTab in App.js

Find where StockTab is rendered in App.js around line 2043.
Remove this prop:
  showToast={showToast}

StockTab already gets showToast from useUI() directly.
It should not also receive it as a prop.

CHANGE 2 — Remove showToast from StockTab props destructuring

In StockTab.js find the component props destructuring.
Remove showToast from it since it now comes from useUI().

CHANGE 3 — Check all toast calls in App.js

Find these lines in App.js and make sure showToast 
is being called correctly:

Line ~2355: showToast("Sale confirmed successfully ✅");
Line ~2391: showToast("Device reserved successfully 🔒");  
Line ~2748: showToast("Device released back to stock 🔓");

These should work since showToast comes from useUI() 
at line 113. But verify the toast JSX is still in App.js.

Find the toast JSX — it looks like:
  {toast && (
    <div style={{ position: "fixed", bottom: ... }}>
      ...
    </div>
  )}

Make sure this JSX still exists in App.js main return.
If it was accidentally deleted during the UIContext 
migration, add it back:

  {toast && (
    <div style={{
      position: "fixed",
      bottom: isMobile ? 90 : 24,
      left: "50%",
      transform: "translateX(-50%)",
      zIndex: 999,
      padding: "12px 24px",
      borderRadius: 12,
      background: toast.type === "success" ? "#10B981"
        : toast.type === "error" ? "#EF4444"
        : "#6366F1",
      color: "#fff",
      fontSize: 13,
      fontWeight: 700,
      boxShadow: "0 4px 20px rgba(0,0,0,0.2)",
      whiteSpace: "nowrap",
      animation: "toastSlideUp 0.3s ease",
    }}>
      {toast.type === "success" ? "✅ "
        : toast.type === "error" ? "❌ "
        : "ℹ️ "}
      {toast.message}
      <style>{`
        @keyframes toastSlideUp {
          from { opacity: 0; transform: translateX(-50%) translateY(16px); }
          to   { opacity: 1; transform: translateX(-50%) translateY(0); }
        }
      `}</style>
    </div>
  )}

After changes:
1. Run npm run build
2. Fix any errors
3. git add -A && git commit -m "fix: toast working with UIContext" 
   && git push origin main --force
4. Test:
   - Reserve a device — does green toast appear?
   - Delete a stock item — does toast appear?
5. Tell me if toast works now