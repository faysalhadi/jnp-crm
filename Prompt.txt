Read src/App.js fully before making any changes.

Fix the Confirm Sale button. Three exact changes needed:

CHANGE 1 — Add handleConfirmSale function
Find the ⚡ Confirm Sale button in the chat window (inside the detail view render). Its onClick currently does something like: setLinkStockDeal(activeDeal); setShowLinkStock(true);

Replace that onClick with: onClick={handleConfirmSale}

Then add this function near the other async handler functions (near moveStage or sendDirectReply):

async function handleConfirmSale() {
  let deal = activeDeal;
  if (!deal && activeCustomer?.id) {
    const { data: newD } = await supabase.from("deals").insert({
      customer_id: activeCustomer.id,
      stage: "new_inquiry",
      brand: "", model: "",
    }).select().single();
    if (newD) {
      deal = newD;
      setActiveDealId(newD.id);
      await loadCustomers();
    }
  }
  if (!deal) return;
  setLinkStockDeal(deal);
  setShowLinkStock(true);
}

CHANGE 2 — Fix onDone callback on LinkStockModal
Find where <LinkStockModal is rendered (inside the JSX, where showLinkStock === true).
Change the onDone prop to:

onDone={() => {
  setShowLinkStock(false);
  setLinkStockDeal(null);
  loadCustomers();
  loadStock();
  refreshCachedStock();
  loadTodaySales();
}}

CHANGE 3 — Guard against null deal inside LinkStockModal confirm()
Inside the LinkStockModal component, find the confirm() async function.
At the very top, after the check for !selItem, add:

if (!deal?.id) {
  alert("No active deal found. Please try again.");
  return;
}

After all three changes: run npm run build, fix any ESLint errors, then git add -A && git commit -m "fix: Confirm Sale button - auto-create deal, refresh on done, null guard" && git push