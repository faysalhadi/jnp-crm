Read src/App.js fully before making any changes.

This is refactor step 7b. Extract HomeTab component.
Do NOT change any logic.

IMPORTANT: File MUST have "import React from 'react';" 
as the very first import.

Create src/components/tabs/HomeTab.js

Add these at the top:
  import React from "react";
  import { STAGES } from "../../constants";
  import { daysSince, timeAgo, monthRevenue } from "../../utils/helpers";
  import Spinner from "../ui/Spinner";

Find the home tab content in App.js:
  {activeTab === "home" && (() => { ... })()}

Extract ALL content inside that block into:
  export default function HomeTab({
    customers, stock, tasks, todaySales, partsRevMTD,
    sourcingAlerts, isMobile, setActiveTab, setView,
    setActiveCustomerId, setActiveDealId, setPendingSuggestion,
    setShowQuickSale, setStockFilter, setFilter,
    setShowAddStock, setEditingStock, setStockForm,
    openDeals, closedDeals, revenue,
    daysSince, timeAgo,
  }) {
    return (
      // exact same JSX that was inside the home tab block
    );
  }

In App.js replace:
  {activeTab === "home" && (() => { ... })()}
With:
  {activeTab === "home" && (
    <HomeTab
      customers={customers}
      stock={stock}
      tasks={tasks}
      todaySales={todaySales}
      partsRevMTD={partsRevMTD}
      sourcingAlerts={sourcingAlerts}
      isMobile={isMobile}
      setActiveTab={setActiveTab}
      setView={setView}
      setActiveCustomerId={setActiveCustomerId}
      setActiveDealId={setActiveDealId}
      setPendingSuggestion={setPendingSuggestion}
      setShowQuickSale={setShowQuickSale}
      setStockFilter={setStockFilter}
      setFilter={setFilter}
      setShowAddStock={setShowAddStock}
      setEditingStock={setEditingStock}
      setStockForm={setStockForm}
      openDeals={openDeals}
      closedDeals={closedDeals}
      revenue={revenue}
    />
  )}

Add import at top of App.js:
  import HomeTab from "./components/tabs/HomeTab";

DELETE the original home tab block from App.js.

VERIFICATION CHECKLIST before building:
- File has "import React from 'react'" at very top
- Exported as default
- Imported in App.js
- Original block deleted from App.js
- No duplicate definitions
- All variables used inside HomeTab are either 
  imported from constants/utils OR passed as props

After changes:
1. Run npm run build
2. Fix ALL errors — if a variable is undefined add it 
   as a prop in both the component and App.js usage
3. Only when build is clean run:
   git add -A && git commit -m "refactor v2 step 7b: extract HomeTab" 
   && git push origin main --force
4. Wait 2 minutes
5. Open https://jnp-crm.vercel.app
6. Check Dashboard — do stats show correctly?
7. Tell me the new App.js line count