JNP CRM — Parked as a real view, and the missing Sourcing section
 
VERIFIED STATE — read this before doing anything
Three files were checked against main at commit 627774d:
  src/components/tabs/CustomersTab.js       — sections are ACT NOW /
      CONFIRM TODAY / STOCK MATCH / FOLLOW UP / RE-ENGAGE / OPEN REQUESTS.
      The words PARKED and SOURCING do not appear at all.
  src/components/tabs/HomeTab.js            — 'parked' appears only in a
      background query feeding re-engage matching. No section.
  src/components/tabs/SalespersonHomeTab.js — neither word appears.
 
Earlier commit messages claimed these sections had landed. They have not.
Do not assume any of this exists — verify, then build what is missing.
 
Read every file fully before editing. Targeted edits only — do NOT rewrite
whole files. Run `npm run build` at the end and fix all ESLint errors.
 
════════════════════════════════════════════════════════════════════════
STEP 0 — REPORT FIRST
════════════════════════════════════════════════════════════════════════
 
  1. grep -rn "PARKED\|SOURCING" src/ — list every hit with its file.
  2. grep -rn "'parked'\|\"parked\"\|'sourcing'\|\"sourcing\"" src/
  3. Confirm STAGES contains 'sourcing' and 'parked' and that 'lost' is gone.
  4. Confirm src/services/parkService.js exists (parkDeal, unparkDeal).
  5. Confirm src/utils/bulk.js exists (sourcingAge, dealTotal, modelKey).
  6. Quote the exact ProfileContext field that gives role / isOwner.
  7. Find the Queue / All view switcher in CustomersTab.js and quote how
     viewMode is stored and rendered.
  8. Is there a "Park" button on any deal today? Yes or no.
 
Print all eight before changing anything.
 
════════════════════════════════════════════════════════════════════════
STEP 1 — PARKED BECOMES A VIEW, NOT A BURIED SECTION
════════════════════════════════════════════════════════════════════════
 
The Clients tab view switcher becomes THREE tabs:
 
      [ Queue (12) ]   [ Parked (14) ]   [ All (104) ]
 
  · Parked sits between Queue and All.
  · The count in the label is live and always visible — that count is the
    whole point. Faisal must be able to see it without opening anything.
  · Follow the existing viewMode pattern exactly as found in step 7.
    Do NOT invent a new navigation mechanism.
 
Do NOT add a collapsed PARKED row to the Queue. Parked is a destination now.
If any earlier attempt left one, remove it.
 
════════════════════════════════════════════════════════════════════════
STEP 2 — THE PARKED VIEW
════════════════════════════════════════════════════════════════════════
 
Contents, top to bottom:
 
── 2a. OWNER SUMMARY STRIP  (owner / manager only — hidden for salesperson)
 
      Ahmed      14 parked · 620 units · avg 22d
      Bilal       6 parked · 180 units · avg 8d
      Kashif     11 parked · 430 units · avg 17d
      Faisal      9 parked · 340 units · avg 15d
 
  · units = SUM(deals.quantity) across that person's parked deals
  · avg   = mean days since parked_at
  · sort by parked count DESC
  · font-variant-numeric: tabular-nums so columns line up
  · quiet styling — small, muted. Reference, not a dashboard.
  · tapping a row applies that person's filter
 
── 2b. FILTER CHIPS  (owner / manager only)
 
      [ Everyone ]  [ Ahmed ]  [ Bilal ]  [ Kashif ]  [ Faisal ]
 
  Horizontally scrollable. "Everyone" selected by default. Names from
  profiles where active = true. Filters by deals.created_by.
 
── 2c. THE LIST
 
  Every customer with at least one deal at stage 'parked'.
  Sort by parked_at, OLDEST FIRST.
 
  Reuse the existing ClientCard. Reason line by parked_reason:
      price_too_high    → `Offered ${money}/unit · ${n} weeks ago`
      not_sourced       → `Couldn't find ${brand} ${model} × ${qty}`
      bought_elsewhere  → `Bought elsewhere · ${n} weeks ago`
      went_quiet        → `Went quiet · ${n} weeks ago`
      not_needed_now    → `Not needed yet · ${n} weeks ago`
      (null/unknown)    → `Parked ${n} weeks ago`
 
  Show quantity and model on every card, same as elsewhere.
  Each card carries an "Unpark" action → unparkDeal(dealId, 'new_inquiry').
 
── 2d. EMPTY STATE
 
      "Nothing parked yet.
       Parked deals stay alive here — they come back when stock lands
       or their price clears."
 
── 2e. SCOPE BY ROLE
 
  Salesperson → only deals whose created_by is himself.
  Owner/manager → everyone, filterable by the chips.
  A deal with created_by NULL stays visible to everyone — legacy rows
  predate that column and must not vanish.
 
════════════════════════════════════════════════════════════════════════
STEP 3 — THE SOURCING SECTION  (in the Queue view)
════════════════════════════════════════════════════════════════════════
 
This genuinely does not exist. Build it.
 
Insert DIRECTLY UNDER "ACT NOW" in the Queue view. Never collapsed.
 
  Label:   SOURCING          dot: amber
  Members: customers with at least one deal at stage 'sourcing'
  Sort:    oldest sourcing_started_at first
 
  Chip from sourcingAge(deal):
      'ok'    → "day 1"                amber
      'warn'  → "day 2"                amber, stronger
      'late'  → "day 3 — close it out" red
 
  Reason line: `Looking for ${brand} ${model} × ${quantity}`
 
  Scope by role the same way as Parked.
 
Faisal loses the customer to another supplier after roughly two days. That
is what these thresholds encode — do not change them.
 
════════════════════════════════════════════════════════════════════════
STEP 4 — THE PARK BUTTON  (only if step 0 found none)
════════════════════════════════════════════════════════════════════════
 
On every open deal (any stage except closed and parked), in the client
detail / right panel: a secondary "Park" button opening a bottom sheet.
 
  Title: "Park this one"
  Sub:   "<client> · <brand> <model> × <qty>"
 
  Five large tap targets from PARK_REASONS:
      💰 Price too high   → reveals a REQUIRED
                            "What did he offer per unit" field
      🔍 Couldn't source it
      🏃 Bought elsewhere
      🤐 Went quiet
      📅 Not needed now
 
  Summary block before confirming:
      ✓ Leaves the queue · follow-up closed
      ✓ <n> units released back to free   (only when a hold exists)
      ✓ Moves to the Parked view
      ✓ Comes back when stock lands or his price clears
 
  Confirm → parkService.parkDeal(dealId, { reason, targetUnitPrice }),
  then refresh the customer list.
 
════════════════════════════════════════════════════════════════════════
STEP 5 — QUEUE PRIORITY
════════════════════════════════════════════════════════════════════════
 
In getQueuePriority (src/context/CustomerContext.js):
  · 'sourcing' ranks just below urgent — above follow-ups.
  · 'parked' returns null. A parked deal must never create a queue entry
    and must never land in OPEN REQUESTS.
 
Confirm no other section picks up parked or sourcing deals as a side effect.
 
════════════════════════════════════════════════════════════════════════
DO NOT
════════════════════════════════════════════════════════════════════════
  · Do NOT put Parked back inside the Queue as a collapsed row.
  · Do NOT collapse or hide SOURCING.
  · Do NOT build a separate owner-only screen — role sets default scope.
  · Do NOT hide deals whose created_by is NULL.
  · Do NOT auto-message anyone.
  · Do NOT enable RLS.
  · Do NOT add npm dependencies.
  · Do NOT rewrite whole files.
 
════════════════════════════════════════════════════════════════════════
FINISH — VERIFY, DO NOT ASSUME
════════════════════════════════════════════════════════════════════════
  1. npm run build
  2. Fix every ESLint error and unused-variable warning
  3. Re-run `grep -rn "PARKED\|SOURCING" src/` and PASTE THE OUTPUT into
     your summary. Do not claim a section exists without showing the grep
     line that proves it.
  4. Also confirm in the summary:
       - the Clients tab renders three view tabs with live counts
       - the summary strip and chips are absent for a salesperson
       - SOURCING sits directly under ACT NOW
       - a parked client appears in Parked and nowhere else
  5. Print files created / edited, and anything you did differently and why.