Read src/App.js fully before making any changes.

This is refactor step 2. Extract helper functions only.
Do NOT change any logic. Do NOT move any components or hooks.

STEP 2 — Create src/utils/helpers.js

Create a new file src/utils/helpers.js

Copy these exact functions from App.js into the new file.
Add "export" before each function definition:

export function getGreeting() {
  // exact same code from App.js
}

export function timeAgo(ts) {
  // exact same code from App.js
}

export function waTsFormat(ts) {
  // exact same code from App.js
}

export function daysSince(ts) {
  // exact same code from App.js
}

export function autoTier(deals) {
  // exact same code from App.js
}

export function monthRevenue(customers) {
  // exact same code from App.js
}

export function getAnthropicKey() {
  // exact same code from App.js
}

export function saveAnthropicKey(k) {
  // exact same code from App.js
}

export function parseGB(str) {
  // exact same code from App.js
}

export function labelGB(gb) {
  // exact same code from App.js
}

export function cleanWhatsAppText(text) {
  // exact same code from App.js
}

Then in App.js:
1. Add this import after the constants import:
   import {
     getGreeting, timeAgo, waTsFormat, daysSince,
     autoTier, monthRevenue, getAnthropicKey, saveAnthropicKey,
     parseGB, labelGB, cleanWhatsAppText,
   } from "./utils/helpers";

2. DELETE the original function definitions from App.js

VERIFICATION CHECKLIST before building:
- Every function is in utils/helpers.js with "export"
- Every function is removed from App.js
- Import statement is at top of App.js
- No duplicate definitions anywhere
- helpers.js has NO imports — these are pure functions

After changes:
1. Run npm run build
2. If errors fix them
3. Only when build is clean run:
   git add -A && git commit -m "refactor v2 step 2: extract helper functions" 
   && git push origin main --force
4. Wait 2 minutes
5. Open https://jnp-crm.vercel.app and confirm it works
6. Tell me the new App.js line count