Read src/App.js and src/components/chat/ChatDetailView.js 
fully before making any changes.

CHANGE 1 — Remove Check Traders button from ChatDetailView

In ChatDetailView.js find and remove:
1. The Check Traders button — it looks like:
   <button onClick={checkTradersForDeal}
   Remove the entire button.

2. The Check Traders results modal/panel — it looks like:
   {showCheckTraders && (
     <div ...>
       ...checkTradersResults...
     </div>
   )}
   Remove the entire block.

3. Remove these from ChatDetailView props destructuring:
   checkTradersForDeal,
   showCheckTraders, setShowCheckTraders,
   checkTradersResults, setCheckTradersResults,
   checkTradersLoading, setCheckTradersLoading,

CHANGE 2 — Remove Check Traders props from App.js

Find where ChatDetailView is rendered in App.js.
Remove these props:
  checkTradersForDeal={checkTradersForDeal}
  showCheckTraders={showCheckTraders}
  setShowCheckTraders={setShowCheckTraders}
  checkTradersResults={checkTradersResults}
  setCheckTradersResults={setCheckTradersResults}
  checkTradersLoading={checkTradersLoading}
  setCheckTradersLoading={setCheckTradersLoading}

CHANGE 3 — Remove checkTradersForDeal from TradersContext

In TradersContext.js:
1. Remove the checkTradersForDeal function entirely
2. Remove showCheckTraders state
3. Remove setShowCheckTraders from context value
4. Remove checkTradersResults state
5. Remove setCheckTradersResults from context value
6. Remove checkTradersLoading state
7. Remove setCheckTradersLoading from context value
8. Remove all of the above from the Provider value object
9. Remove useCustomers import since it was only 
   needed for checkTradersForDeal

Also update TradersProvider signature back to:
  export function TradersProvider({ children, anthropicKey, activeDeal }) {

And in index.js restore TradersProvider to NOT 
use CustomerContext — remove the useCustomers dependency.

CHANGE 4 — Remove Check Traders from TradersTab

In TradersTab.js find and remove:
1. The Check Traders button in the UI
2. The Check Traders results display
3. These from props/hook destructuring:
   showCheckTraders, setShowCheckTraders,
   checkTradersResults, setCheckTradersResults,
   checkTradersLoading, setCheckTradersLoading,
   checkTradersForDeal,

After ALL changes:
1. Run npm run build
2. Fix ALL errors
3. git add -A && git commit -m "remove: check traders feature from chat and traders tab" 
   && git push origin main --force
4. Test:
   - Client chat opens without errors
   - Traders tab works normally
   - No check traders button visible anywhere
5. Tell me new App.js line count