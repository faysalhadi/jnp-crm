Read src/App.js fully before making any changes.

This is refactor step 6. Extract modal components one by one.
Each modal gets its own file. Do NOT change any logic.

STEP 6A — Create src/components/modals/PartSaleModal.js

Create a new file src/components/modals/PartSaleModal.js

Add these at the top:
  import React, { useState } from "react";
  import { supabase } from "../../supabase";

Copy the exact PartSaleModal function from App.js:
  export default function PartSaleModal({ part, onClose, onComplete }) {
    // exact same code
  }

STEP 6B — Create src/components/modals/LinkStockModal.js

Create a new file src/components/modals/LinkStockModal.js

Add these at the top:
  import React, { useState, useEffect } from "react";
  import { supabase } from "../../supabase";
  import Spinner from "../ui/Spinner";
  import { parseGB, labelGB } from "../../utils/helpers";

Copy the exact LinkStockModal function from App.js:
  export default function LinkStockModal({ customer, deal, onClose, onDone }) {
    // exact same code
  }

STEP 6C — Create src/components/modals/SpecUpgradeModal.js

Create a new file src/components/modals/SpecUpgradeModal.js

Add these at the top:
  import React, { useState } from "react";
  import { parseGB, labelGB } from "../../utils/helpers";

Copy the exact SpecUpgradeModal function from App.js:
  export default function SpecUpgradeModal({ item, onClose, onApply }) {
    // exact same code
  }

STEP 6D — Create src/components/modals/ReservationModal.js

Create a new file src/components/modals/ReservationModal.js

Add these at the top:
  import React, { useState } from "react";
  import { supabase } from "../../supabase";
  import Spinner from "../ui/Spinner";
  import { parseGB, labelGB } from "../../utils/helpers";

Copy the exact ReservationModal function from App.js:
  export default function ReservationModal({ customer, deal, stock, onClose, onDone }) {
    // exact same code
  }

STEP 6E — Create src/components/modals/QuickSaleModal.js

Create a new file src/components/modals/QuickSaleModal.js

Add these at the top:
  import React, { useState } from "react";
  import { supabase } from "../../supabase";
  import { parseGB, labelGB } from "../../utils/helpers";

Copy the exact QuickSaleModal function from App.js:
  export default function QuickSaleModal({ stock, onClose, onComplete, prefill = null }) {
    // exact same code
  }

STEP 6F — Update App.js imports

Add these imports at the top of App.js:
  import PartSaleModal from "./components/modals/PartSaleModal";
  import LinkStockModal from "./components/modals/LinkStockModal";
  import SpecUpgradeModal from "./components/modals/SpecUpgradeModal";
  import ReservationModal from "./components/modals/ReservationModal";
  import QuickSaleModal from "./components/modals/QuickSaleModal";

Then DELETE these functions from App.js:
  - PartSaleModal
  - LinkStockModal
  - SpecUpgradeModal
  - ReservationModal
  - QuickSaleModal

VERIFICATION CHECKLIST before building:
- Every modal file has "import React from react" at top
- Every modal file has all necessary imports
- Every modal is exported as default
- All five modals are removed from App.js
- All five are imported in App.js
- No duplicate definitions anywhere
- parseGB and labelGB are imported from utils/helpers 
  in files that need them

After changes:
1. Run npm run build
2. Fix ALL errors one by one
3. Only when build is clean run:
   git add -A && git commit -m "refactor v2 step 6: extract modal components" 
   && git push origin main --force
4. Wait 2 minutes
5. Open https://jnp-crm.vercel.app and confirm it works
6. Test: open a client chat and tap Confirm Sale — 
   does the modal appear?
7. Tell me the new App.js line count