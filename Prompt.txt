Read src/App.js and src/components/tabs/StockTab.js 
fully before making any changes.

CHANGE 1 — Pass filteredStock to StockTab

In App.js find where StockTab is rendered.
It starts with:
  {activeTab === "stock" && (
    <StockTab

Add this prop to the StockTab usage:
  filteredStock={filteredStock}

CHANGE 2 — Add filteredStock to StockTab props

In src/components/tabs/StockTab.js find the component 
function signature:
  export default function StockTab({

Add filteredStock to the destructured props list.

CHANGE 3 — Fix StockTab to use filteredStock prop

In StockTab.js find any place that tries to compute 
filteredStock locally or uses it without it being in props.
Make sure it uses the filteredStock passed from App.js.

After changes:
1. Run npm run build
2. Fix any errors
3. git add -A && git commit -m "fix: pass filteredStock prop to StockTab" 
   && git push origin main --force
4. Wait 2 minutes
5. Open https://jnp-crm.vercel.app
6. Check Stock tab — do devices show now?
7. Tell me if it works