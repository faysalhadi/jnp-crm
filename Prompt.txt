In src/App.js find the handleConfirmSale function and add this as the very first line inside it:

  alert('handleConfirmSale called, activeDeal: ' + JSON.stringify(activeDeal) + ' activeCustomer: ' + JSON.stringify(activeCustomer?.id));

Then npm run build, git add -A && git commit -m "debug: add alert to handleConfirmSale" && git push origin main --force