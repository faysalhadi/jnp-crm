Read src/App.js fully before making any changes.

Find this broken button in the file — it is inside the chat detail view, inside the section marked with the comment "⚡ CONFIRM SALE — always visible above inputs":

The button currently looks like this (broken — missing its opening tag):

          <div style={{ marginBottom: 8 }}>
            >
              ⚡ Confirm Sale — Link Inventory
            </button>
          </div>

Replace that entire div block with this fixed version:

          <div style={{ marginBottom: 8 }}>
            <button
              onClick={handleConfirmSale}
              style={{
                width: "100%",
                padding: "12px 16px",
                borderRadius: 12,
                border: "none",
                background: "#6366F1",
                color: "#fff",
                fontSize: 14,
                fontWeight: 800,
                cursor: "pointer",
              }}
            >
              ⚡ Confirm Sale — Link Inventory
            </button>
          </div>

Do not change anything else. After the fix run npm run build, fix any ESLint errors, then git add -A && git commit -m "fix: restore missing button tag on Confirm Sale" && git push origin main --force